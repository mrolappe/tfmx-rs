[package]
name = "tfmx-web"
description = "Browser playback core for tfmx: plain-Rust core plus a wasm boundary"
publish = false
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
tfmx = { path = "../tfmx" }
