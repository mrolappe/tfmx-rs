# Roadmap

> ## ▶ Status
>
> | | |
> |---|---|
> | **Next step** | **▶ START HERE (2026-08-03, session 21): Phase 5.7 (sample + sampler-instrument export) is done — structurally verified, one real gap disclosed below.** New `tfmx-cli export-instruments` subcommand and a new `tfmx-cli/src/export/` module implement `docs/m5-plan.md`'s "structure first" design exactly: one `InstrumentSerializer` trait over 5.3's zone table, three formats (`wav`, `sfz`, `dspreset`) each in their own file and registered by name (`export::by_name`/`FORMAT_NAMES`), so a fourth format needs one new file plus one registry line. `build_instrument` resolves a macro's `ZoneTable` and fetches each sample-bearing zone's live PCM via `Module::sample`, skipping per-zone (not per-instrument) on an out-of-bounds region; key/velocity ranges reuse `midi.rs`'s own pitch anchor and formulas (`MIDDLE_C_TFMX`/`MIDDLE_C_MIDI`/`velocity_for`, promoted to `pub(crate)` rather than re-derived a second place) so raw note `0x18` = MIDI 60 = every exported sample's pitch-keycenter, all written at TFMX's native 8363Hz rate. `tfmx_analysis::SampleRegion` gained a `looped: bool` field (threaded from `SamplePointer`'s `loop_active`) so exporters know one-shot vs. sustain-loop per zone -- a real correctness need, not speculative, TDD'd against real corpus data (`turrican intro` macro 28's `$18`-terminated chain now pins `looped: true`). The WAV writer is hand-rolled RIFF (`fmt `/`data`/optional `smpl` loop chunk) since `hound` has no custom-chunk support. **Check results, disclosed honestly**: no sfizz/Kontakt/DecentSampler installed in this environment, so the phase's own "loads in sfizz *and* Kontakt"/"loads in DecentSampler" criteria are **not** closed in the tool-verification sense -- only structurally: 10 new unit tests (TDD'd), plus an end-to-end run against `turrican intro` song 0 in all three formats independently checked with macOS's own `afinfo` (confirms valid 8-bit/8363Hz PCM) and `xmllint --noout` (confirms well-formed `.dspreset` XML) -- external tools, not this crate re-checking its own output, but not the sampler apps the criterion actually names. Whoever has those apps available should complete that pass before treating 5.7 as fully closed. Full workspace suite green (90 `tfmx-cli` tests, up from 83), clippy clean (the same one pre-existing unrelated warning as every prior phase, not touched -- two new findings in the new code were fixed, not left), `wasm32-unknown-unknown` build for `tfmx` unaffected, golden hashes unchanged. Full account in `docs/m5-session-log.md`'s Phase 5.7 entry; ledger entry `M5-08` updated to `done`. **Per this file's phase-gate rule: stop here, wait for explicit approval before starting Phase 5.8** (visualization). Superseded text below kept for reference. **Superseded 2026-08-03, session 20: Phase 5.6 (fidelity scoreboard) is done — both `check:` criteria pass.** New `tfmx-cli fidelity-scoreboard` batch-renders the 10-module corpus through this crate's own `render_to_wav` and, side by side, through `uade123` (a GPL reference player *executed* for A/B comparison, permitted by this file's own hard rule — its code was not read), then scores each pair with the *existing* `onset-diff`/`measure-pitch` machinery (`detect_onsets`/`pearson_correlation`/`measure_pitch_hz`), extracted into a new pure `compute_module_fidelity` function so the metric math is unit-testable without a reference player or the corpus on disk. Phase 5.1's WinUAE register-log spike was confirmed unusable as an automated oracle (manual capture, unresolved trust risk), so this uses the plan's own "audio metrics otherwise" branch. Writes a tracked `docs/fidelity-scoreboard.json` (one `onset_correlation`/`our_pitch_hz`/`reference_pitch_hz` entry per module) carrying the plan's honesty requirement verbatim in its own `honesty_note` field. **Finding, disclosed rather than silently shipped**: the pitch fields collapsed to a constant, meaningless ~8820Hz on 9 of 10 modules — autocorrelation over a whole dense polyphonic mix locks onto its own shortest allowed lag, the same failure this project's fidelity-thread history already flagged for `measure-pitch` at full-mix scope; the honesty note says so explicitly rather than reporting it as data. `onset_correlation` does not share this problem and ranges informatively from -0.37 to 0.99 across the corpus. **Check results**: `fidelity_scoreboard_runs_across_full_corpus_without_error` actually ran the full batch this session (both `uade123` and the corpus were present) and confirmed all 10 modules produce valid JSON; `compute_module_fidelity_mutation_moves_onset_correlation` is the plan's own "a deliberate known-bad mutation moves the metric" check (identical rhythms correlate ~1.0, a re-clustered mutation drops it by >0.5), written test-first per this project's TDD rule. 3 new tests, full workspace suite green (83 `tfmx-cli` tests, up from 80), clippy clean (one pre-existing unrelated warning, not touched), `wasm32-unknown-unknown` build for `tfmx` unaffected (only `tfmx-cli` touched, no new dependency — `uade123` is invoked as an external process). Full account in `docs/m5-session-log.md`'s Phase 5.6 entry; ledger entry `M5-07` updated to `done`. **Per this file's phase-gate rule: stop here, wait for explicit approval before starting Phase 5.7** (sample + sampler-instrument export). Superseded text below kept for reference. **Superseded 2026-08-03: Phase 5.5 (MIDI export) is done — both `check:` criteria pass.** `tfmx-cli export-midi` exports a song trace to a Standard MIDI File through a hand-editable JSON mapping (`tfmx-cli/src/midi_mapping.rs`) auto-drafted from Phase 5.3's zone tables: `(macro, note range, velocity range) → program | drum note | drop`. New `tfmx-cli/src/midi.rs` builds the note-event stream directly from a real trace (the same `render_traced` seam `trace` uses, extracted into a shared `render_trace` helper) rather than the static walk, so timing is exactly what the song actually plays: one MIDI tick per jiffy (trivially exact-integer per the plan's own requirement), tempo meta events on tempo change, one `NoteOn` per `Trigger` closed by the next retrigger/`KeyUp`/trace-end. Vibrato/portamento surface as pitch bend derived from observed `Voice.period` deltas against a per-trigger reference (not from decoding `$0B`/`$0C` specifically — more general, catches any period-moving opcode). Written via `midly` (added as a `tfmx-cli`-only dependency, `rayon`/`parallel` off). **Check results**: all 10 corpus modules export MIDI that round-trips through `midly::Smf::parse` with note count matching the trace's own `Trigger` count exactly; editing the mapping (dropping a triggered macro's zones) provably changes the exported notes. 19 new tests (test-first per this project's TDD rule), full workspace suite green (80 `tfmx-cli` tests, up from 61), clippy clean (one pre-existing unrelated warning, not touched), `wasm32-unknown-unknown` build for `tfmx` unaffected (only `tfmx-cli` touched). Full account in `docs/m5-session-log.md`'s Phase 5.5 entry; ledger entry `M5-06` updated to `done`. Not done, explicitly out of scope: nobody has yet opened the exported file in a real DAW/player by ear — structurally verified only; `$08`/`$09`/`$1E`/`$1F` macro-internal note changes aren't decoded into separate `NoteOn`s, only surfaced as pitch bend via the period-delta mechanism. **Per this file's phase-gate rule: stop here, wait for explicit approval before starting Phase 5.6** (fidelity scoreboard). Superseded text below kept for reference. **Superseded 2026-08-03: Phase 5.4 (JSON dump + serialization seam) is done — both `check:` criteria pass.** `tfmx-analysis`'s `serde` feature (carried unused since Phase 5.2) is now wired up: `#[cfg_attr(feature = "serde", derive(serde::Serialize))]` on every public walker/zone type (`WalkResult`, `Span`/`SpanKind`, `SampleRegion`, `Zone`/`ZoneTable`/`ZoneExit`/`MacroVolume`/`Envelope`); stays optional/default-off. New `tfmx-cli dump --format json` runs `walk_song` + `resolve_zones` for every reachable macro and serializes the result (mirrors `trace`'s value-enum-with-one-variant-so-far seam). New `trace --format json` fills the `TraceFormat` TODO at `tfmx-cli/src/main.rs:766`: `TraceEvent` lives in the dependency-free `tfmx` core (hard rule, no `serde` there), so its JSON encoding is hand-written in a new `tfmx-cli/src/serialize.rs` (ndjson, one `serde_json::json!` arm per variant) rather than derived; `write_trace` now takes a `format` parameter and switches internally, one function per format as the file's own existing doc comment called for. **Check results**: all 10 corpus modules' `dump --format json` output is valid, re-parseable JSON with a non-empty `zones` array; `cargo build -p tfmx-analysis --no-default-features` still compiles, same structs exposed. New tests: `walk_result_serializes_to_valid_json`/`zone_table_serializes_to_valid_json` (`tfmx-analysis`, feature-gated), `write_trace_json_emits_one_valid_json_object_per_line`/`dump_json_is_valid_and_has_zone_tables_across_full_corpus` (`tfmx-cli`, the latter mirroring `lint`'s existing corpus-loop shape). Full workspace suite (65 relevant tests) green, clippy clean (one pre-existing unrelated warning, not touched), `wasm32-unknown-unknown` build for `tfmx` unaffected. Full account in `docs/m5-session-log.md`'s Phase 5.4 entry; ledger entry `M5-05` updated to `done`. **Per this file's phase-gate rule: stop here, wait for explicit approval before starting Phase 5.5** (MIDI export, user-prioritized). Superseded text below kept for reference. **Superseded 2026-08-02: Phase 5.3 (zone resolution, `$1C`/`$1D`) is done — both `check:` criteria pass, delegated to an Opus 5 agent per the plan's model tag.** New `tfmx-analysis/src/zones.rs`: `resolve_zones(module, macro_number) -> ZoneTable`, a symbolic pass over a macro's bytecode that partitions the `0..=$3F` (note) × `0..=64` (entry volume) rectangle into disjoint zones by interpreting `$1C <Splitkey>`/`$1D <Splitvol>` as interval cuts rather than concrete branches. `$1C` cuts the note axis directly (no opcode writes the note register); `$1D` tracks the volume register as `clamp(entry + offset, lo, hi)` since `$0D`/`$0E`/`$1E` touch it and clamping does not compose with addition — a single accumulated offset would be wrong. `$0F <Envelope>` (time-varying volume) or a revisited step yields `ZoneExit::Unresolved` rather than a guess. `walk_song` and its tests are untouched; `SamplePointer`/`sext24` visibility widened to `pub(crate)` for reuse. New fixture `testdata/synth/gen_split_probe.py` (+ generated `mdat`/`smpl.split-probe`, `testdata/synth/` un-ignored) for a from-scratch known-boundary probe. **Check results**: `turrican intro` macro 28 (no `$1C`/`$1D`) resolves to exactly one full-rectangle zone matching `disasm`'s linear structure; the probe macro resolves to exactly two zones split at the right note boundary. Corroborating coverage: macro 24's real keysplit, macro 5's `$1D` chain, and an all-128-macros/all-10-modules point-coverage test. 240 workspace tests pass (up from ~231), clippy clean, `wasm32-unknown-unknown` build for `tfmx` unaffected (only `tfmx-analysis` touched). Full account, including the finding below, to be added to `docs/m5-session-log.md`'s Phase 5.3 entry by a future session if not already done; ledger entry for 5.3 needs updating to `done`. **Open finding, not acted on (fidelity question, not a 5.3 decision)**: `turrican intro` macro 5's `$1D` chain (`$0D +$15` then four `$1D`s at `$20/$2A/$34/$3C`) reads as dead code (3 of 4 `Cont` targets unreachable) under the documented "jump if volume < aa" polarity, but as a clean 5-way velocity-layered fan-out under the opposite polarity ("jump if volume >= aa") — very suggestive the documented polarity is backwards, but the runtime/decode was deliberately left unchanged since 5.3's check criterion requires matching *current* behavior; test `turrican_intro_macro_5_splitvol_chain` documents today's reading so it's the one to update if this is investigated and the polarity flips. Also noted: macro 28's `$0E <SetVolume>` has `aa=$00` (volume resolves to 0, `cc=$38` unused) — mirrors the interpreter, not investigated further. **Update (2026-08-02): the open finding above is RESOLVED, a confirmed engine bug now fixed.** An ear-check fixture (`testdata/synth/gen_splitvol_probe.py`) settled the polarity question against the real TFMX editor: triggering a `$0D +0`-then-`$1D` macro at two note volumes straddling the threshold played the *fallthrough* branch for the lower volume and the *jump* branch for the higher one — the reverse of `docs/opcodes.md`'s literal "jumps if volume is less than `aa`". Also confirmed: `$1D` only reads a meaningful volume once an explicit `$0D`/`$0E` has run since trigger — a bare `$1D` as a macro's first opcode always jumped regardless of volume in the real editor, unlike this crate's `trigger()` (which seeds the volume register eagerly); no real corpus macro observed puts `$1D` before priming, so that's a model-accuracy footnote, not a fix. Fixed `tfmx/src/macro_interp.rs`'s `$1D` (`< b1` → `>= b1`, TDD'd) and `tfmx-analysis/src/zones.rs`'s mirrored branch (taken set is now a volume-axis *suffix*, not a *prefix*); `turrican_intro_macro_5_splitvol_chain` rewritten for the resulting 5-zone ascending fan-out (quietest → macro 4 … loudest → macro 0), confirming the finding's own prediction. `docs/opcodes.md:177` and a new note below its table record the corrected polarity. Full workspace tests, clippy, `wasm32-unknown-unknown` all pass; `tfmx-cli`'s golden-hash suite is byte-identical — traced and confirmed macro 5 is never triggered within any corpus module's first 90s of any song (statically reachable in `turrican intro` song 2, but only via a `$06 Cont` chain from macros not observed triggered there), so this fix has no effect the existing regression net could see either way; a real in-song A/B of macro 5's chain is still open for whoever finds where it actually plays. Full account in `docs/m5-session-log.md`'s Phase 5.3 entry. **Per this file's phase-gate rule: still stop here, wait for explicit approval before starting Phase 5.4** (JSON dump + serialization seam). Superseded text below kept for reference. **Superseded 2026-08-02: Phase 5.2 (static walker core, `tfmx-analysis`) is done — all its `check:` criteria pass.** Per the fresh session's re-decide on Phase 5.1's spike (see the superseded entry below): the user chose to treat the spike's finding as sufficient and move straight to the walker rather than invest further in oracle automation. New crate `tfmx-analysis` (workspace member, depends only on `tfmx`, `serde` an optional default-off feature not yet wired to any type) implements `walk_song(module, song) -> WalkResult`: BFS worklist over trackstep lines → patterns (via `TrackSlot::Pattern`, `$F2 Jump`/`$F8 GoSub`/`$FB PlayPattern`) → macros (via `Note.macro_number`, `$06 Cont`/`$15 Go submacro`/`$21 Play macro`), linear single-pass scan per entry mirroring `tfmx-cli disasm`'s own shape (lists to the terminator, does not execute branches) — yielding reachable pattern/macro sets, an `mdat` byte-provenance span per visited entry, best-effort `smpl` sample-region tracking from `$02`/`$03`/`$11`/`$12`/`$18`/`$19`, and raw (unmasked) voice-nibble collection with an `is_7v()` detector. Two small `tfmx` core additions the walker needed, both TDD'd, no new dependency: `Module::pattern_offset`/`macro_offset` (the absolute `mdat` offset `pattern`/`macro_` slice from, needed for provenance spans) and `sequencer::decode_line` made `pub` (re-exported as `tfmx::decode_line`, mirroring `decode_pattern_entry`'s existing stateless-decode seam). **Corpus result**: all 10 corpus modules walk song 0 without panic; `apidya (title)` is the only one whose raw voice nibbles include 4-7 with no 3, confirming the 7V signature; per-module `mdat` provenance coverage ranges 16.7%-60.9% (expected to be well under 100% — only song 0 walked, header/pointer tables aren't pattern/macro bytecode; see `m5-plan.md`'s own interpretation note). 7 new `tfmx-analysis` unit tests, 2 new `tfmx` unit tests; full workspace suite, clippy and `wasm32-unknown-unknown` build for `tfmx` all still pass. Full account, including the one seeded-test-bug wrong turn (a hand-encoded byte with volume/voice nibbles swapped, caught by its own assertion), in `docs/m5-session-log.md`'s Phase 5.2 entry; ledger entry `M5-03` updated to `done`. **Next: Phase 5.3, zone resolution (`$1C`/`$1D`) — the milestone's spine** (`docs/m5-plan.md`'s Phase 5.3 block) — per-macro `(note range, velocity range) → sample region + envelope summary`, everything downstream (MIDI, SFZ/sampler, sample export) consumes this table. Read `docs/m5-plan.md`'s Phase 5.3 block before starting; it is tagged Opus 5 (ambiguous interval-algebra work, not mechanical). **Superseded 2026-08-02: Phase 5.1's re-decide question** (WinUAE Memwatch spike done with a positive but capture-manual, trust-unverified result — full writeup in `docs/m5-session-log.md`'s Phase 5.1 entry) **is answered**: move on, oracle automation and the WinUAE-vs-fs-uae inconsistency stay open but unblocking, for whoever wants that oracle later. Everything below this sentence is the PAUSED fidelity thread, kept for reference — do not resume it without the user asking.** **Update (2026-08-01, session 14): direction change, chosen by the user.** Next up is **brainstorming, then planning, the "Export and static analysis" milestone** (see the "Later milestones" section below) -- not more troubleshooting on the thread this cell used to lead with (that thread is **paused, not closed**; see the redirect at the bottom of this cell). Scope per the roadmap's own one-liner: "all of it wants a *static* module walker that resolves a song to its reachable patterns, macros and sample regions, rather than M4's runtime seam" -- machine-readable module dump, sample export (the `smpl` file has no directory; regions exist only as `$02`/`$03`/`$18` operands inside macros, so this is an analysis job, not a file split), round-trippable text disassembly (assemble -> identical `mdat` would be the strongest parser validation this project could have), MIDI/SFZ export, piano-roll and structure diagrams, cross-module sample fingerprinting. **Process for the fresh session that picks this up**: brainstorm the shape of this milestone first (what's actually worth building, in what order, what a "static module walker" API should look like) -- do not jump straight to a phased plan or to code. Once there's a settled shape, write it up as a phased plan (mirroring the M1-M4 style already in [ROADMAP-history.md](ROADMAP-history.md): one deliverable + diagrams + a `check:` verification per phase) and get it approved before implementing, per this file's own working agreement below. **Relevant existing context, not a pre-made design**: `tfmx-cli disasm` already emits human-readable pattern/macro disassembly opcode-by-opcode -- read it first as the closest existing thing to "text disassembly," and note it likely isn't designed to be round-trippable (re-assemblable) yet. `tfmx/src/module.rs` is the on-disk parser a static walker would sit on top of. The hard rule that `tfmx` (the core crate) stays dependency-free, allocation-free after load, I/O-free and `wasm32`-buildable almost certainly means new export/analysis tooling belongs in `tfmx-cli` (or a new crate), not the core crate -- confirm this against [`docs/architecture.md`](docs/architecture.md) during brainstorming rather than assuming it. Read `docs/architecture.md` for the existing code shape before proposing where new pieces go. **The macro-playback-fidelity troubleshooting thread is paused here, not abandoned -- resume it later via these pointers, don't re-derive:** **§15 (`docs/macro-playback-fidelity.md`), not implemented**: voice 2's macro-internal pulse rate measured ~3.5x too slow (onset-diff against a user-recorded editor reference, pitch already ruled out as a confound). Working theory: TFMX may have two independent clock domains (trackstep/pattern tempo-scaled, already confirmed; macro interpreter's `$04`/`$0F`/`$0B`/`$0C`/`$11` timers on the raw 50Hz rate instead) -- `docs/playback-model.md` §1's "one unified clock" claim is uncited and untested for the macro side. Reproduction steps (reference recording path, exact CLI commands, expected numbers, concrete next step) are recorded at the end of §15 so this doesn't need to be re-derived. **§1 (`docs/macro-playback-fidelity.md`), CONFIRMED, still not fixed**: pattern-record portamento-to-note (`aa > $BF`) is decoded into `NoteTiming::Portamento(dd)` correctly but then silently dropped by `dispatch_pattern_entry` -- no glide is ever rendered. Needs a design decision first: how `dd` maps onto the existing open-ended `Portamento` effect, which has no target/arrival concept today. **§3 (`docs/macro-playback-fidelity.md`), OPEN, unconfirmed**: pattern `0x6b`/macros `0x0a` and `0x27`, "timbre/sound not correct" -- never root-caused. Smaller **Uncertain** items with no active lead, recorded in the docs where they live: `$EFFE 0002 SetTempo` precedence when both `divisor`/`CIA bpm` are set (`playback-model.md` §3.3); trackstep hi-byte values `$81`-`$FD` (tolerantly handled, meaning unconfirmed); the `v` nibble in the pattern note record's `cv` byte; `$FE <StCu>`'s distinguishing behavior vs. `$F4`; the `v > 15` CIA/BPM tempo path (never reliably tested -- the editor didn't react properly to tempo-field edits in the one attempt). Full history of the paused thread below, oldest issue first: **New thread opened 2026-07-31, independent of the trackstep-timing work below: live editor cross-check of individual macros found a CONFIRMED bug** -- `dispatch_pattern_entry` (`tfmx/src/player.rs:382-420`) decodes a pattern note's portamento-to-note form (`aa > $BF`) into `NoteTiming::Portamento(dd)` correctly but then silently drops it, routing it through the same `note_on` call as an ordinary immediate trigger -- no glide is ever rendered. Corroborated by the user's ear on `turrican intro` pattern `0x6b` step 9. Two more leads (macro-preview tempo mismatch; a suspicious monotonic sample-pointer growth in macro `0x0a`) are open but unconfirmed. A new `tfmx-cli render-macro` subcommand was added to audition a single macro directly, bypassing trackstep/pattern/transpose -- see its `--tempo` gotcha (defaults to 50Hz, not any song's actual tempo). Full findings, exact trace excerpts and next steps: [`docs/macro-playback-fidelity.md`](docs/macro-playback-fidelity.md). **This does not block or depend on the trackstep-timing item below** -- both are open, pick either first. **Update (2026-08-01), read `docs/macro-playback-fidelity.md`'s §5 intro first**: two more bugs on this same thread were found, fixed, TDD'd and structurally verified (a `$04 <Wait>` zero-operand suspend bug, and a `Paula::set_sample_region` bug that undid the one-shot-attack-to-loop handoff every jiffy) -- but the user's re-listen found **no audible difference**, "sounds more or less the same as before". A third bug was then found and root-caused but **not fixed**: pattern `0x52`/macro `0x1c` on voice 0 is inaudible because a `$18 <Sampleloop>` computes a loop region that reads far past the end of `smpl.turrican intro` (129536 bytes requested against a 45828-byte file), silently returning digital zero via `Paula::next_sample`'s `unwrap_or(0)` fallback. Whoever picks this up next should not assume the two structurally-verified fixes are perceptually meaningful, or that this third bug is the whole story either -- form a theory for the disconnect before continuing (the still-unfixed §1 portamento-drop bug is a live suspect too). Separately, the trackstep-timing fix still needs: regenerate the golden hashes and get the user's ears on it. Both of the TFMX editor's questions on that thread are now answered (see the 2026-07-31 update below). The "different melody" complaint has a root cause: `docs/opcodes.md` §2 states at *documented* confidence that `$F0 <End>` advances the trackstep, but `Player::run_jiffy` advanced the shared line pointer **unconditionally every tick**, on a comment that claimed to resolve `docs/playback-model.md` §7 and justified itself with "mostly `$80 Hold` words". A corpus count kills that premise: **5 of 10 modules contain zero `$80 Hold` words**, `turrican intro` among them, while the mean pattern runs 30-460 ticks and was given exactly **one**. Roughly **98% of the composed music never played** -- right instruments, wrong tune -- which also explains the 71% superseded-trigger rate, the all-voice silence gaps, the `no-retrigger` lint finding and the 4.4s loop. Fixed behind `TrackstepGate::{AllTracks, AnyTrack}` (`tfmx-cli render|trace --gate all|any`) because §7's aggregation question is genuinely unsettled; 5 tests, mutation-checked against both an always-advance and an over-strict mutation; the `track_pattern` shadow field deleted. **A second, coupled defect is now exposed and measured**: autocorrelating `uade123`'s 180s reference envelope gives six top periods that are *all* exact integer multiples of **0.64s = 32 jiffies at 50Hz**, but song 0's tempo is genuinely 3 (header tables verified aligned), which §3.2's cited `50/(v+1)` makes 12.5Hz -- incapable of that grid. **Update (2026-07-27): question 1 answered, settled by direct test in the TFMX editor.** Two tracks authored on one trackstep line, one with a fixed 2-jiffy pattern and the other with a 200-jiffy pattern on a sustaining (loop-until-key-up) instrument: the line advanced in under 500ms regardless -- driven entirely by the *short* track's `$F0`, with the long track's length (2 jiffies vs. 200, a 100x swing) making no difference at all. Only `AnyTrack` predicts that; `AllTracks` is ruled out. `TrackstepGate`'s `#[default]` flipped from `AllTracks` to `AnyTrack` (`tfmx/src/player.rs`), doc comments updated to record the finding; all 138 `tfmx` unit tests still pass (none depended on the old default beyond single-track lines, where the two readings agree). **Update (2026-07-31): question 2 answered too, and the "second, coupled tick-rate defect" very likely never existed.** Six trackstep lines authored in the editor, each holding one track playing pattern 21 (`Wait(31); End` = 32 jiffies exactly), timed at two speed settings: speed 3 -> 15.4s (192 jiffies -> 12.47Hz measured vs. `50/(3+1)` = 12.50Hz predicted, 0.26% off) and speed 2 -> 11.5s (16.70Hz measured vs. `50/(2+1)` = 16.67Hz predicted, 0.17% off); a length-independent ratio cross-check (`15.4/11.5 = 1.339` vs. predicted `4/3 = 1.333`) confirms it a second way. **The editor's "speed" field is the stored tempo value, and `50/(v+1)` holds** -- already exactly what `tick_fraction` in `tfmx/src/sequencer.rs:38-45` implements, so no tick-rate code change is indicated. That undercuts the earlier "second defect": its 0.64s-grid reading used a 20ms autocorrelation hop that is *itself* exactly one 50Hz jiffy, so "every peak is an integer number of 50Hz jiffies" was true by construction regardless of the real rate -- not discriminating evidence. Re-ran the same autocorrelation against a fresh 180s `uade123` render and checked the top 12 peaks against the now-confirmed 12.5Hz instead: every one lands on an exact integer jiffy count there too (in fact all multiples of 2 jiffies), at least as clean as the original 50Hz reading. **Not yet proven beyond doubt** (still no ear confirmation, per the standing rule) but there is no surviving evidence against the code's existing formula. Full account in [`docs/trackstep-timing-bug.md`](docs/trackstep-timing-bug.md) §3. **Next**: regenerate the golden hashes for all ten corpus modules (expected to change under the `AnyTrack` default) and get the user's ears on a full-mix + per-voice-stems A/B against `uade123` before this counts as done. **Postponed, not blocking**: a same-day attempt to measure the `v>15` CIA/BPM tempo path (speed 0x78/0xA0) looked like a real directional falsification of `50/(v+1)`'s sibling formula, but the user then noticed the editor doesn't always react properly to tempo-field changes, so that data point can't be trusted and was shelved rather than recorded as a finding (`docs/trackstep-timing-bug.md` §4) -- moot for now since every corpus module's default song uses tempo <=15. **Update (2026-08-01): full-mix + per-voice-stems A/B against `uade123` finally happened (golden hashes regenerated, `--gate any`, 90s) -- verdict: not yet matching.** The user heard real similarity but named concrete problems: voice 0 and voice 2 both have wrong pitch *and* wrong speed, voice 0 additionally sounds like "the playhead moves to the wrong places in the sample file"; voice 3 sounds correct. This matched `tfmx-cli lint`'s `sample-region-out-of-bounds` finding exactly (voices 0 and 2 flagged, corroborating `docs/macro-playback-fidelity.md` §5) -- traced to a real bug in `$18 <Sampleloop>` (`tfmx/src/macro_interp.rs`): the 24-bit delta is a *byte* value (same units as `$02 SetBegin`) but was subtracted from `loop_len` unconverted, even though `loop_len` mirrors Paula's *word*-counting length register (`$03 SetLen`'s own units) -- breaking `docs/opcodes.md` §4's own stated invariant ("the sample's end point is unchanged"). Fixed by halving the delta (`>> 1`, matching this codebase's existing 68000-ASR rounding convention) before the subtraction; verified against the exact root-caused case (end point now provably unchanged: `39960` bytes before and after) and corpus-wide (`sample-region-out-of-bounds` findings drop from 26 to 9 voice-instances across all ten modules). TDD'd: two existing tests corrected to use word-aligned deltas and the right expected values, two new tests added (one pinning the end-point invariant directly, one regression-pinning the exact `turrican intro` macro-28 case). All 142 `tfmx` unit tests, `mutation_robustness` and `tfmx-cli`'s suite pass; golden hashes regenerated again for the six modules whose render window actually exercises an odd/impactful `$18` delta. **Re-listened: voice 0/2 sound no different either** -- same disconnect pattern as the earlier §3/§4 fixes. **Built a `tfmx-cli measure-pitch` tool (autocorrelation-based Hz detector, TDD'd) and used it on a from-scratch isolated single-macro test (no loops/effects/trackstep) to confirm `note_period()`+Paula's period register are correct end-to-end** (three notes an octave apart measured 525.0/1050.0/2100.0 Hz against a predicted 522.7/1045.4/2090.8 Hz -- octaves double exactly, the constant ~0.44% offset is the autocorrelation's own integer-lag quantization, not a real error). This rules out the core per-note pitch formula as the "off everywhere" cause and redirects suspicion onto whether `$18`'s resulting loop-length *values* (not just their in-bounds-ness, already fixed) are actually correct -- unprovable without the editor as ground truth. **CHOSEN NEXT STEP**: a concrete two-recipe test plan (replay the already-root-caused pattern `0x52`/macro `0x1c` case directly and listen for sustained-tone vs. silence; a manufactured fallback mirroring the exact real delta if that's too ambiguous) is recorded in `docs/macro-playback-fidelity.md` §8 -- read that first in a fresh session. **Update (2026-08-01, session 2): Recipe A run, two more real bugs found+fixed+TDD'd on the same voice, `turrican intro` voice 0's `sample-region-out-of-bounds` finding is now fully gone -- but the user's re-listen still finds voice 0 too low-pitched and still hears the playhead wander into the wrong sample areas.** Recipe A (editor playback of pattern `0x52`/macro `0x1c`) corroborated §5: a clear tone under the pattern's retrigger rhythm, not silence. Tracing the still-present lint finding (not the loop region itself, which the editor confirmed correct) found two further bugs in `tfmx/src/macro_interp.rs`, both fixed and TDD'd: (1) `$11 <AddBegin>`'s one-shot form kept wobbling the *stale pre-loop* `sample_start` and re-pushing it through `Paula::set_sample_region` every jiffy even after `$18` had handed the voice to loop playback, undoing the §4 handoff and dragging the real read pointer past the file -- fixed with a `loop_active` flag redirecting the wobble to `loop_start` once set; (2) `$05 <Loop>`'s shared `self.repeat` counter was poisoned by the unconditional (`aa=0`) form's `get_or_insert(0)`, which planted a stale `Some(0)` that the next *counted* loop instruction read as "already exhausted," running one pass instead of its own six -- the actual mechanism behind the previously-unconfirmed "monotonic sample-pointer growth" lead, now confirmed. 144 `tfmx` unit tests, full workspace suite, clippy and `mutation_robustness` all pass; golden hashes regenerated for the 4 modules whose audio changed (`turrican intro`, `apidya (title)`, `turrican 2 level 1-desert`, `turrican 2 title (st)`). **Both fixes are structurally correct (regression-tested) but did not resolve the user's ear complaints** -- per this thread's repeated pattern, a bounds-respecting fix is not the same as a value-correct one. Full writeup, including 3 concrete not-yet-tried next steps (test whether the `$11` wobble's own magnitude -- 5x the loop region's size -- is the "wanders" cause by temporarily no-op'ing it; get the editor's actual loop-point values for macro 28, §8 Recipe A step 3, never attempted; isolate macro 28 via `render-macro`+`measure-pitch` against the editor's own macro-audition), in `docs/macro-playback-fidelity.md` §9. 10 `sample-region-out-of-bounds` findings remain across other corpus modules, unexplored. **Update (2026-08-01, session 8): tried isolating macro 28 via `render-macro`+`measure-pitch` (§9's third next step) -- found only a benign tooling gotcha, no code changed.** `render-macro --macro 28` renders ~60ms of audio then hard silence for the rest of the file, so the `measure-pitch` reading taken from it was worthless. Traced (temporary, reverted `eprintln!`): macro 28's own `$0E <SetVolume> aa=$00` genuinely zeroes its volume once -- not a decode bug, `docs/opcodes.md` §2 confirms `$0E`'s `aa xx xx` layout and the real data really is `aa=0` there. Harmless in-song: pattern `0x52` retriggers macro 28 every 1-3 jiffies, and `note_on`'s "same macro, still running" branch (`tfmx/src/macro_interp.rs:389-397`) overwrites `self.volume` from the pattern's own data on every retrigger without resetting the macro's step counter, so this `$0E` fires exactly once, ever, and is overwritten again within 60ms regardless. `render-macro`'s single, un-retriggered trigger has no such recovery -- same failure shape as §2/§6's earlier tooling gotchas, not a playback bug. Written up as §10 in `docs/macro-playback-fidelity.md`. **User chose theory 1 next** (does the `$11 AddBegin` post-loop wobble's own magnitude -- 5-6x the loop region's size -- explain "pitch too low"/"wanders"?): read `docs/macro-playback-fidelity.md`'s "CHOSEN NEXT STEP (session 9)" section for the exact, ready-to-run recipe. **Update (2026-08-01, session 9): theory 1 executed, mixed result -- rules out the wobble as the "too low pitch" cause, doesn't rule it out for "wanders."** No-op'd the wobble (throwaway edit, reverted, `git status` clean), rendered before/after pairs at pattern/full-mix/voice-0-solo scope, quantified: the diff is real and large everywhere (43.7%/35.6%/30.8% nonzero samples, 113%/46%/60% RMS-of-diff ratio -- same order of magnitude as the confirmed-real §7 fixes), so the wobble is not a dead branch. But the one *unconfounded* measurement -- pattern 82 alone, no trackstep/gating cascade -- shows **identical measured pitch (441.00 Hz) and identical onset rhythm (20→20 onsets, correlation 1.000) before and after**. The full-mix/voice-0 onset-count and correlation changes are large too, but per §7's established caveat that pattern (diff at every scope, inconsistent direction) is what the `AnyTrack` gating cascade produces on its own, not specific evidence for this fix -- and voice-0's `measure-pitch` reading (8820 Hz, unchanged) is not trusted as a real single-note pitch measurement at that scope (90s solo render, many notes and silences). Full writeup, including the not-yet-spent user-ear step, in `docs/macro-playback-fidelity.md` §11. **Next**: either A/B the rendered pairs by ear specifically for "wanders" (numbers only cleared the pitch question), or skip straight to theory 2 (editor loop-point ground truth for macro 28, §9) since it's the only remaining lead for "too low pitch" specifically. Rendered WAV pairs are in the session scratchpad, not committed. **Ear result (same session): the user A/B'd voice 0 and theory 1 is dead, not just neutral — disabling the wobble made it sound worse**, "the pad in voice 0 before [wobble on] sounded better, having some kind of modulation sweep. the voice 0 after [wobble off] version sounds like the loop is looping over a smaller section, thus sounding faster and wrong/grainy." The wobble is a wanted sweep/chorus effect, not the "wanders" bug — do not remove or weaken it. This also means the original "wanders" complaint was never about this code path; its real cause is still unidentified. New lead for theory 2: the no-wobble loop alone reads by ear as smaller/faster/grainier than the real instrument, i.e. possibly too *short*, which is the opposite direction of "too low pitch" if the loop length sets the fundamental — worth asking the editor's loop-point inspector directly whether `loop_len=128` words (macro 28) underShoots the composer's intended region. **CHOSEN NEXT STEP: theory 2** (get the editor's own loop-point/playhead values for macro 28, §9) — the only remaining lead for "too low pitch," now with this extra data point to check against. **Update (2026-08-01, session 10): theory 2 answered — no decode bug, and the `$18` fix's formula is now independently validated by ear, not just self-consistency.** The editor has no loop-point inspector, so the user instead read macro 28's raw instructions off the editor's own disassembly (`SetBegin +0x1C14`, `SetBegin +0x7804`, `SetLen 0x100`/`0x400`, `Sampleloop +0x700`) — matches `tfmx-cli disasm --macro 28` byte-for-byte, ruling out a decode bug. Recipe B (a minimal hand-built macro mirroring the real numbers, swept around the real `0x700` delta) showed delta-below-`0x700` sounds "slower", approaching `0x700` sounds "faster", and at/above `0x800` other regions of the sample file (including silence) start playing — this matches the code's `loop_len = sample_len − (delta>>1)` formula exactly, including the precise cliff (`loop_len` hits exactly 0 at `delta=0x800` for this macro's `sample_len=1024`, then wraps mod 65536). Closes out "wrong `$18` loop-length values" as a suspect. **New theory found and recorded, explicitly not pursued this session (user's choice)**: `docs/opcodes.md` describes `$02 SetBegin` as adding to "the sample's base address" (fixed) vs. `$11 AddBegin` adding to "the sample pointer" (live/oscillating) — two different phrases the code currently treats as one and the same accumulating field. Macro 28's two `SetBegin` calls are the one case in this thread where that distinction would actually change which bytes get read, not just bounds — the strongest remaining lead for "too low pitch"/"wanders", but unproven (one sentence per opcode, no corroborating passage) and contradicts an already-committed test. Full write-up: `docs/macro-playback-fidelity.md` §12. **Update (2026-08-01, session 11): theory acted on, real fix landed, ear complaint not resolved.** `$02 SetBegin` made absolute (per `docs/playback-model.md` §2's own table) instead of cumulative, TDD'd; a corpus-wide scan first found 60+ macros across 6 modules issue `$02` twice/three times per trigger (not just macro 28), and `tfmx-cli lint`'s `sample-region-out-of-bounds` findings dropped from 9 to 3 (the 3 remaining are all in the out-of-scope TFMX 7V `apidya (title)`). 145 `tfmx` unit tests, full workspace suite, `mutation_robustness`, clippy pass; golden hashes regenerated for the 4 modules whose first 10s changed. **But the user's ear A/B found no improvement**: "it does not sound pitch correct (and overall the other voices also do not sound pitch correct). and the wandering also does not seem to be fixed" -- and that *other, unrelated* voices are also pitch-wrong is new information reframing the complaint as possibly not opcode-specific at all. Kept and committed anyway (user's explicit call, independently justified by the docs/corpus evidence). Full writeup: `docs/macro-playback-fidelity.md` §13. **User is now pivoting the diagnostic strategy**: build a minimal from-scratch test song (a few trackstep lines, one track, one pattern playing a musical scale, through the simplest possible macro -- a non-looping or simply-looping sound, synthesized or lifted from `smpl.turrican intro`) to isolate pitch from every remaining real-corpus confound by construction. This is the chosen next step. **Update (2026-08-01, session
11 continued): minimal test song built and A/B'd against the real TFMX editor — likely root cause
found, not yet fixed.** `testdata/synth/` (tracked in git, generated not copyrighted) holds
`gen_minimal_scale.py` and its output: a from-scratch song, one pattern, a 13-note chromatic scale
through the simplest possible macro, over a synthesized sine-cycle sample. Silent in the editor at
first (fixed by moving the tone off `smpl` offset 0 -- `docs/format.md` §8's "null sample"
convention -- and switching tempo 0 -> 3, an editor-validated tempo); once audible, pitch-correct
in this crate's own renderer (all 13 notes within ~1% of predicted) but **consistently higher-
pitched in the real editor for identical note bytes**. The user's own controlled experiment in the
editor pinned it exactly: note `$1E` in this crate's render matches note `$18` in the editor's, and
the same six-semitone (tritone) gap reproduces at the octave's other end (`$2A`/`$24`) -- in an
equal-tempered system, two such matching pairs only happen with a uniform, note-independent offset.
Re-fetched `[S1]` itself (Pickard's *TFMX Professional 2.0 Song File Format*) to check for a
transcription error: none found, `docs/playback-model.md`'s citation is verbatim -- but `[S1]`
gives only the one anchor point (`$1E`=middle C=8363Hz) and a name table, **never an actual
note->frequency formula**; the exponential formula is this project's own inference (already flagged
as such), and every prior validation of `note_period()` in this whole thread checked that formula
only against itself (ratios, octave-doubling) -- which a uniform wrong anchor constant satisfies
perfectly while still being globally wrong. This is the first time pitch has been checked against
real hardware/editor ground truth in the entire investigation. Checked `tfmx/src/paula.rs`'s mixer
resampling math as an alternative fix location -- clean, no hidden factor found -- so the likely fix
is `tfmx/src/macro_interp.rs`'s `const MIDDLE_C_NOTE: i32 = 0x1E;` -> `0x18` (keeping
`MIDDLE_C_HZ = 8363.0` as-is). **Not yet implemented.** This is foundational and wide-blast-radius
(shifts every note in every module), unlike every earlier fix in this thread -- full plan for what
needs updating (doc worked examples, absolute-period test assertions, all ten golden hashes, a
re-check of `testdata/synth/mdat.minimal-scale` against the editor before spending corpus-wide
listening time) recorded in `docs/macro-playback-fidelity.md` §14. **This is the chosen next step
for a fresh session.** Also worth noting for whoever picks this up: it has a real mechanism
matching the original complaint's shape (wrong pitch on *every* voice, not just one macro's data),
unlike every earlier per-opcode theory in this thread -- promising, but per this thread's repeated
lesson (§7/§9/§13), do not trust it structurally-passing tests alone; get the user's ears on it.
**Update (2026-08-01, session 12): the change implemented, TDD'd, structurally verified -- not yet
heard.** `MIDDLE_C_NOTE` changed `0x1E` -> `0x18` in `tfmx/src/macro_interp.rs`; five tests that
hard-coded an absolute period at the old anchor updated (renumbering the note bytes each test
triggers, not just the expected period); `docs/playback-model.md` §4's worked examples and prose
moved to the `$18` anchor (`$1E` keeps its table name "C-3" -- only the frequency anchor moved).
145 `tfmx` unit tests, full workspace suite, `mutation_robustness` and clippy all pass; golden
hashes regenerated for **all ten** corpus modules (every note's pitch shifts, unlike every earlier
narrower fix in this thread). Rendered, not yet listened to: `testdata/synth/mdat.minimal-scale`
(session scratchpad) and a full-mix + 4-stem `turrican intro` render (`--seconds 90 --gate any`,
session scratchpad). **Update (2026-08-01, session 13): pitch confirmed correct by ear on the full
mix. A second, separate, independently-confirmed bug found while re-checking §2.** Voice 2's
macro-internal pulse rate (pattern `0x54`/macros `0x30`-`0x31`) is measured **~3.5x too slow**:
`onset-diff` against a user-recorded editor reference (pitch-matched, 155.28 vs 154.20 Hz, ruling
out a timbre confound) gives 1.9 onsets/s (this crate) vs. 6.2 onsets/s (editor); autocorrelation
of the editor recording's envelope finds a clean 160ms fundamental period, while a reverted
throwaway test confirms this crate's own macro-interpreter loop is a *correct* implementation of
the documented opcodes at 7 jiffies/cycle = 560ms (12.5Hz song tempo). **Working theory, not yet
implemented**: TFMX may have two independent clock domains -- trackstep/pattern tempo-scaled (per
the already-confirmed `docs/trackstep-timing-bug.md` formula) but the macro interpreter's own
`$04`/`$0F`/`$0B`/`$0C`/`$11` timers running at the *raw* 50Hz hardware rate regardless of song
tempo -- `docs/playback-model.md` §1's "one unified clock" claim is uncited and was never actually
tested against real hardware for the macro side until this session. 140ms (7 raw jiffies) is much
closer to the measured 160ms than the current model's 560ms, but not an exact match, and this
would be a wide-blast-radius change (decoupling the macro tick clock throughout `player.rs`/
`sequencer.rs`), unlike every earlier per-opcode fix in this thread. **Not yet implemented --
chosen next step for a fresh session, pending the user's direction.** Full writeup:
`docs/macro-playback-fidelity.md` §15. |
> | **Previously** | **Step C round 2 is done: `note_period()`'s formula is FALSIFIED as the cause, but two real caller bugs were found and fixed -- the fix needs the user's ears (full mix + per-voice stems, A/B against `uade123`) before it counts as done.** Hypothesis: a systematic error in `note_period()` (`tfmx/src/macro_interp.rs`) would give right rhythm and wrong pitch on every module, i.e. exactly the "different melody" symptom. Checked coverage first: its five existing tests are *all five of `docs/playback-model.md` §4's own worked examples*, so tests and implementation came from the same four points and could not catch a formula that fits them and drifts elsewhere -- 59 of 64 reachable notes untested, the 8-bit/16-bit detune combination untested, the pattern record's `dd` path untested. **Formula: falsified.** New sweep over all 64 notes against an independently derived expectation (iterating the literal 2^(1/12), deliberately never calling `powf`) plus the octave-halving invariant on all 52 pairs: **max deviation 0**, well inside the ±1 the doc itself leaves open on rounding. Both tests mutation-checked (perturbing the exponent divisor 12.0 -> 12.02, a 0.17% error, fails them) so they demonstrably have teeth. **Callers: two real bugs, both fixed TDD.** (1) The pattern note record's `dd` detune was decoded into `NoteTiming::Detune` and then **silently dropped** -- `dispatch_pattern_entry` destructured `timing` away with `..`; it now flows through `note_on` into the same `self.detune` slot `$21` already used (which also fixes a leak found in passing: a `$21` detune used to outlive its note and bleed into later ones). (2) `note_period(note, word23 as i16 + self.detune)` is an unguarded `i16` add over **raw module data** -- `word23 >= $7F81` plus a positive `$21` detune panics in debug, violating `tfmx/tests/mutation_robustness.rs`'s never-panic contract; now `saturating_add`. **Audible size of the fix, measured, not assumed**: traced every plausible song slot of all ten modules (`--seconds 90`) -- only 422 of ~150 000 executed notes carry a non-zero `dd`, every value between `+1` and `+11` (`+0.4%`..`+4.3%`, at most 3/4 of a semitone), seven modules unaffected entirely, `turrican intro` renders **byte-identical**, and all ten golden hashes are unchanged (no regeneration needed). So the fix is right but cannot be the "different melody" cause -- that would need tens of percent. **The investigation stays open**; per the standing rule this is not "done" until the user has listened. Full account in `docs/status.md`'s "Update (2026-07-26): Step C round 2 -- `note_period()` pitch mapping" section. |
> | **Before that** | Step C round 1: **hypothesis falsified, no code changed.** The user chose hypothesis 3 -- pattern/macro-number and track/voice mapping. Concretely testable form: `decode_pattern_entry` (`tfmx/src/sequencer.rs:519-535`) reads the `cv` byte as `volume: cv >> 4` / `voice: cv & 0x0F`, and `voice_of` (`tfmx/src/player.rs:20-22`) then masks that nibble with `& 0x03` -- both flagged **Uncertain** in their own doc comments, and any real `v` value of 4-15 would be silently wrapped onto the wrong voice on every module. Checked test coverage first: the `cv` *split* is pinned by `tfmx/src/sequencer.rs:1255-1345` and `:1774-1848` against `docs/format.md` §6's worked examples, but every `v` in them is already 0-3 and `voice_of` has no test at all -- the masking was untested guesswork. Then swept `tfmx-cli trace` over **every plausible song slot of all ten corpus modules** (`--seconds 90`), counting the raw pre-mask nibble of each executed `Note`: **111 846 note entries across the nine four-voice modules, zero with a nibble outside 0-3** -- the mask is a no-op on every module the user flagged, so this cannot be the "different melody" cause. **No code changed.** Two keepers: `apidya (title)` (the one TFMX 7V module, out of scope) uses exactly seven `v` values -- 0,1,2,4,5,6,7, never 3 -- which is the first real-data support for reading `v` as a channel selector at all; and a static `disasm --pattern 0..127` scan *does* show nibbles up to 15, but only in slots past the last pattern any song references (decoded garbage, not music data -- do not mistake it for a lead). Per the plan, exactly one hypothesis was tested and the session stops: whether to instrument `note_period()` or the `cv` split next is the user's call. Full account in `docs/status.md`'s "Update (2026-07-26): Step C -- pattern/macro-number and track/voice mapping -- **hypothesis falsified**" section. |
> | **Phase** | 11 of 11 (M4) — Diagnostics tooling — complete. Phase gate's chosen repair is a genuine partial fix, landed and committed; the underlying "different melody" complaint remains open. **Still stop for explicit approval before starting the next milestone.** |
> | **Gate** | M3 complete and approved. M4/Phase 11 (planned and approved 2026-07-26) delivered the diagnosis; the gate then chose to repair `turrican intro`'s confirmed bug next (`apidya (title)` remains separately explained as TFMX 7V, unsupported, `docs/architecture.md` §9). **One real bug found, fixed, and committed**: every macro in this module opens with `$00 aa=0` (mandatory 1-jiffy pause) and ends its note-setting opcode on another 1-jiffy suspend, so `$01 DMAon` needs two clear jiffies after a trigger. `dispatch_pattern_entry` called `MacroInterpreter::trigger()` (a full reset) for every `Note` event regardless of whether the same macro was already running on that voice — a fast note run retriggering the same macro every jiffy reset `step` back to 0 each time, so `$01` was never reached. Confirmed real (not benign) by an A/B against `uade123`: this crate's voice 1 was completely silent (`rms=0.0`) over the run's whole 0.5-1.5s span while the reference had continuous energy there. Fix: `MacroInterpreter::note_on` (`tfmx/src/macro_interp.rs`) — same macro number + still running (not yet `$07`-stopped) → update note/volume/transpose in place instead of `trigger()`; otherwise unchanged. Post-fix, `tfmx-cli lint` reports no findings for `turrican intro` (was `no-retrigger` + clipping), and all ten corpus golden hashes changed and were regenerated. **But the user reports the full render still sounds very different from `uade123` after this fix** — so this bug was real but is not the (whole) explanation for the original A/B complaint. **Song-number mismatch ruled out**: `uade123 -g` confirms subsong 0 is its default (matching our `--song 0`); this crate's own song table (`tfmx-cli info`) shows slot 0 = lines 75-129/tempo 3, and 27 of the 32 slots are an identical placeholder (`50/50`/tempo 5, confirmed by trace to decode as a bare `$EFFE 0000 Stop`) that both tools evidently stop enumerating at the same boundary (uade reports exactly 5 subsongs, 0-4). Cross-checked independently by note density: this crate's song-0 trigger rate (26.1/s) matches `uade123`'s measured onset rate (27.1/s) far better than song 1 (81.2/s) or song 2 (132.2/s) would. Full account in `docs/status.md`'s "Update (2026-07-26): `turrican intro`'s confirmed bug fixed" section. |
> | **Last done** | Implemented `$FD <Lock>` and `$FB <PPat>` pattern commands, `tfmx/src/player.rs`. Both were previously recognized-but-no-op (`$FA <Fade>` in the same bucket was already fixed in an earlier session). TDD: `Lock` arms a per-voice jiffy countdown that drops `Note` dispatches for that voice while non-zero. `PlayPattern` redirects another track to a new pattern; collected during the per-track dispatch pass and applied once after every track has run that jiffy -- this single-pass ordering alone reproduces `docs/opcodes.md` §2's "own track lower than target: next entry / otherwise: immediate" rule with no extra bookkeeping. Found and fixed a real bug surfaced by the `PlayPattern` integration test: `Sequencer::track` resolves a `$80 <Hold>` word using its *own* remembered pattern number, independent of any pattern-level jump, so the existing reload check (`patterns[i].pattern() != number`) silently undid the jump on the next Hold; fixed with a new `Player::track_pattern` field tracking what the trackstep itself last assigned, separate from the live `PatternRunner`. `PlayPattern`'s own `transpose` field is decoded but deliberately left unapplied (documented gap: no source states which of the pattern-level transpose vs. the trackstep's own per-jiffy transpose wins). Six new unit/integration tests, all pass; full workspace suite, clippy, and all ten corpus `lint`/golden-hash checks unaffected (neither opcode appears in the corpus outside `apidya (title)`'s 75 `Lock` calls, which render byte-identical before/after -- no competing note ever lands in one of its lock windows). Not part of the M1-M4 step list; a real gap found during this investigation, done ad hoc like the earlier master-volume-slide addition. |
>
> Update this block in the same commit that ticks a checkbox.

Progress tracker and the authoritative step list. **Tick a box in the same commit that
completes the step.** `git log` records what happened; this file records what is next.

Each step names its deliverable, its verification, and the **minimum model** recommended to
implement it. "Minimum" means a smaller model is likely to get this subtly wrong; a larger
one is always fine.

## Working agreement

- Working language for code, comments, docs, commit messages and file names: **English**.
- One commit per completed step. Subject names the step, e.g. `docs: add format.md (step 1.1)`.
- **Stop at the end of every phase** and wait for explicit approval before starting the next.
- Documentation carries diagrams: Mermaid for processes and state machines, ASCII tables for
  on-disk byte layouts.
- **No GPL source is ever read.** Every existing TFMX replayer is GPL-2.0; this code is written
  from the published spec. Reference players are executed as black boxes for A/B listening
  only. See the provenance section of [README.md](README.md).

## Delegating a step

Each step below is written to be handed over **verbatim and on its own**. An agent working a
step gets exactly this and nothing more:

1. The step's own block (deliverable, diagrams, check) — not the other steps, not the phase
   history, not the conversation that led here.
2. The relevant entries from [Sources](#sources), and the already-written files in `docs/`
   that its step builds on.
3. The hard rules that bind it: **never read GPL replayer source**, English only, and for core
   code — no dependencies, no allocation after load, no I/O, no threads.
4. Its verification criterion, and the instruction to run it before reporting done.

Do not pass this file wholesale, the plan history, or prior steps' reasoning. An agent that
needs the roadmap to understand its task has been given a task that is not yet well specified
— sharpen the step instead of widening the context.

---

## Completed milestones

M1 through M4 are done. Their full step-by-step detail — deliverables, verification criteria,
design decisions and the `Finding from X` notes recorded along the way — moved to
[ROADMAP-history.md](ROADMAP-history.md) so routine work on remaining/future topics doesn't load
it by default. Pull it up when a step's rationale or a past gotcha is actually needed (e.g. before
touching the trace seam, the trackstep-advance timing, or `note_period()`/detune handling again).

- **M1** — Documentation (`docs/format.md`, `docs/opcodes.md`, `docs/playback-model.md`,
  `docs/architecture.md`), parser, Paula mixer, sequencer, `tfmx-cli`, golden-hash regression
  tests, A/B listening pass. Phases 0–6.
- **M2** — `tfmx-play`, a `cpal`-based desktop realtime player with transport controls. Phase 7.
- **M3** — `tfmx-web`, a `wasm-bindgen`/`AudioWorklet` web player with a no-bundler demo page.
  Phases 8–10.
- **M4** — Diagnostics tooling: voice mute/solo/stems, `TraceEvent`/`render_traced`, `tfmx-cli
  trace` and `lint`, mutation-robustness fuzzing. Phase 11. Plus ad hoc fixes found along the way
  (`$FB`/`$FD` pattern commands, `turrican intro`'s macro-retrigger bug, `note_period()` caller
  bugs) — all detailed in the history file. The diagnosis this milestone produced fed directly into
  the still-open investigation described in the Status block above.

---

## M5 — Export and static analysis (brainstormed, planned and **approved 2026-08-02**; not started)

**Full brief: [`docs/m5-plan.md`](docs/m5-plan.md)** — read it before starting any phase below. It
carries the context, the six recorded decisions with their rationale, the architectural
constraints, the 7V posture, the delegation rules and each phase's subtask/model breakdown. The
list here is the checkbox tracker only.

Two findings made during planning shape the whole milestone, both verified, neither obvious:

- **A reference Paula register log looks obtainable.** WinUAE 6.1.0 (Stefan Reinauer's
  cross-platform fork) is installed at `/Applications/WinUAE.app`, has a `Memwatch` debugger and
  symbolic `AUD0LCH/LCL/LEN/PER/VOL`. Paula's audio registers are memory-mapped at
  `$DFF0A0–$DFF0DF`, so a write-watch there *is* a reference log — captured from the **TFMX editor**
  the user already trusts, with no GPL replayer involved. This would be the project's first cheap
  oracle. Unverified risk: whether memwatch can *log* rather than *break*, and be captured to file.
- **Macros are key/velocity zone maps.** `docs/opcodes.md:176-177` — `$1C <Splitkey>` branches on
  note, `$1D <Splitvol>` on volume. Resolving those statically gives, per macro, a
  `(note range, velocity range) → sample region` table that maps **one-to-one onto SFZ's
  `lokey`/`hikey`/`lovel`/`hivel`**. That one table feeds MIDI mapping, sampler export, sample
  export and tracker export alike. It is the spine of the milestone.

Phases, each with its minimum model. Every phase ends with the standing ritual in the brief:
update the idea ledger, append problems *and mistakes* to `docs/m5-session-log.md`, update the
Status block to name the next phase, commit, push, **stop**.

- [x] **5.0** Idea ledger (`docs/analysis-tooling-ideas.md`) + session log skeleton — *Haiku 4.5*
- [x] **5.1** Reference register-log spike, **timeboxed, then re-decide** — *Opus 5*
- [x] **5.2** Static walker core, new `tfmx-analysis` crate; reachability, byte-provenance, free 7V
      detector — *Sonnet 5*
- [x] **5.3** Zone resolution (`$1C`/`$1D`) — the spine — *Opus 5*
- [x] **5.4** JSON dump + serialization seam; fills the `TraceFormat` TODO at
      `tfmx-cli/src/main.rs:766` — *Sonnet 5*
- [x] **5.5** MIDI export (user-prioritized), mapping keyed on `(macro, note range, velocity
      range)`, auto-drafted from 5.3 — *Sonnet 5*
- [x] **5.6** Fidelity scoreboard — regression detection, **not** a truth oracle — *Sonnet 5*
- [x] **5.7** Sample + sampler-instrument export behind an `InstrumentSerializer` trait: WAV
      `smpl` chunk, SFZ, DecentSampler `.dspreset` — *Sonnet 5*
- [ ] **5.8** Visualization, with the **view-model structs as the deliverable** and HTML as one
      replaceable consumer — *Sonnet 5*

Deferred ideas (probe-module generator, opcode census, static linter, smpl directory
reconstruction, macro fingerprinting, trace explorer, spectrograms, web explorer, tracker export,
score export) go into 5.0's ledger as `proposed`, not into this list.

## Later milestones

- **Beyond:** TFMX 7V support (a separate parser path — the format is substantially different,
  not a flag; see the 7V posture in [`docs/m5-plan.md`](docs/m5-plan.md), which explains why
  "7 voices" does *not* mean widening the existing 4-element arrays), GemX macro opcodes, tracker
  TUI in `tfmx-play`, web visualizer in the M3 demo.

## Sources

Cite these by tag when briefing a step.

| Tag | Source | What it gives |
|---|---|---|
| **S1** | [libxmp `docs/formats/tfmx-format.txt`](https://github.com/libxmp/libxmp/blob/master/docs/formats/tfmx-format.txt) — J. H. Pickard, *The TFMX Professional 2.0 Song File Format* | The authoritative spec: header layout, trackstep, pattern and macro opcode listings, the note table |
| **S2** | [RetrovertApp/playback-tfmx `TFMX.md`](https://github.com/RetrovertApp/playback-tfmx/blob/master/TFMX.md) | Background on 7V, a worked macro dump, scope notes. **Prose only — the surrounding repo is GPL-2.0, do not read its code.** |
| **S3** | [ExoticA wiki: TFMX](https://www.exotica.org.uk/wiki/TFMX) | Context, module inventory |
| **S4** | [VGMPF: MDAT](https://www.vgmpf.com/Wiki/index.php?title=MDAT) / [SMPL](https://www.vgmpf.com/Wiki/index.php?title=SMPL) | Short format overviews, cross-check only |
| **S5** | `testdata/` (fetch with `sh testdata/fetch.sh`) | 10 real modules, 5 packed / 5 fixed layout |

## Known risks

| Risk | Mitigation |
|---|---|
| Timing model (CIA timer vs. 50 Hz divider) — the most common failure, and it sounds like a working player at the wrong speed rather than like a bug | 50 Hz path first, CIA path added separately and verified by ear |
| Two header layouts | Both from the start; detection is the zero check found in 0.3 |
| GPL contamination | Reference players are executed, never read |
| Aliasing from naive resampling | Accepted for M1, marked with a `ponytail:` comment |
